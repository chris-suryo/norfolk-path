# norfolk-path

_What this is and why it exists - run /grill then /plan to fill this in._

## Status

- next: (not set)

## Locations

The single place you (or any AI session) look for where this project's pieces
live, so nothing is ever hunted for. Fill in the real paths; delete rows that
don't apply.

| Piece | Where |
|---|---|
| Repo | `E:\code\norfolk-path` |
| Runtime / data | (config, models, a database - else delete this row) |
| Related services | (an API/hub this talks to, and where it runs - else delete) |

**On-disk standard:** code repos live under the code root `E:\code\<name>` (new
projects land there via `dos new`). If this project deliberately keeps files
anywhere else, record it above so it never becomes a surprise.

---
kit: 364605cbbd6bbff3e9ed81ee4fe49035120c7ff0 · stamped by dos new
